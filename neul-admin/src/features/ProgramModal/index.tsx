import ProgramWrite from "../ProgramWrite";
import { ProgramModalStyled } from "./styled";

const ProgramModal = (props: { list: any }) => {
  const { list } = props;

  //console.log("list", list);
  return <ProgramModalStyled></ProgramModalStyled>;
};

export default ProgramModal;
